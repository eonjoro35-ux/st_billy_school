# Only needed if the API is deployed at a different domain than the frontend.
# During local development, Vite proxies /api to http://localhost:5000 automatically
# (see vite.config.ts) and this can be left unset.
# VITE_API_BASE_URL=https://api.yourschool.org/api
